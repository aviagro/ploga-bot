# העלאת השינויים לבוט

## דרך מהירה (מומלץ)

בטרמינל, מתוך תיקיית הפרויקט:

```bash
cd /Users/avia/ploga-bot
chmod +x upload.sh
./upload.sh
```

הסקריפט יעשה: `git add` → `commit` → `push` ל-GitHub.

---

## אחרי ההעלאה — Render

1. היכנס ל-[dashboard.render.com](https://dashboard.render.com)
2. בחר **ploga-bot**
3. **Manual Deploy** → **Deploy latest commit**
4. חכה ל-**Live** (ירוק)

### משתני סביבה (לעדכון נשק)

| Key | Value |
|-----|--------|
| `TELEGRAM_TOKEN` | כבר אמור להיות |
| `SHEET_CSV_URL` | `.../export?format=csv` |
| `GOOGLE_SERVICE_ACCOUNT_JSON` | כל תוכן קובץ ה-JSON מ-Google Cloud |
| `ALLOWED_EDITOR_IDS` | (אופציונלי) מספר הטלגרם שלך |

5. **Save** → Deploy מחדש אם הוספת משתנים

---

## בדיקה בטלגרם

| פקודה | מה אמור לקרות |
|--------|----------------|
| `/help` | רשימת פקודות כולל `/setweapon` |
| `/setweapon` | מתחיל עדכון נשק |
| חיפוש שם | עדיין עובד כמו קודם |

---

## אם הסקריפט נכשל

```bash
git status
git remote -v
```

ודא שיש חיבור ל-GitHub (`origin`). אם צריך התחברות — התחבר ב-GitHub Desktop או `gh auth login`.
